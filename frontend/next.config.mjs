/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.scdn.co",
      "s1.ticketm.net",
      "mosaic.scdn.co",
      "images.universe.com",
    ],
  },
};

export default nextConfig;
